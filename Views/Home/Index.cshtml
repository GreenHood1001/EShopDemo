

@model dynamic

@{
    ViewData["Title"] = "Home Page";
}


<div class="container" style="height: 30vw;">
        <div class="gallery js-flickity responsive" data-flickity-options='{ "cellAlign": "center", "contain": true }'>
            <div class="gallery-cell"><img src='~/img/ofertas_banner.jpg' class="responsive"/></div>
            <div class="gallery-cell"><img src='~/img/sale_banner.jpg' class="responsive"/></div>
            <div class="gallery-cell"><img src='~/img/ofertas_banner.jpg' class="responsive"/></div>
        </div>
</div>
<div class="container">
    <div style="margin-bottom: 2vw;">
        <h3>Categorias</h3>
        
            <div class="main-carousel" data-flickity='{"groupCells": true, "cellAlign": "left", "contain": true }'>
                @foreach (Categoria categoria in Model.Categorias)
                {
                        <div class="carousel-cell">
                            <form method="GET" action="Categoria" id="@categoria.Name"> 
                                <input type="hidden" value="@categoria.ID" id="valor">  
                                <a href="javascript:{}" onclick="document.getElementById('@categoria.Name').submit();">
                                    <div class="card bg-dark text-white">
                                        <img class="card-img" src="@categoria.imageData" alt="@categoria.Name">
                                        <div class="card-img-overlay">
                                            <h5 class="card-title">@categoria.Name</h5>
                                        </div>
                                    </div>
                                </a>
                            </form>
                        </div>
                }
            </div>
        
    </div>
    <div>
        <h3>Recomendaciones</h3>
        <div class="main-carousel" data-flickity='{"groupCells": true, "cellAlign": "left", "contain": true }'>
                @foreach (Usuario usuario in Model.Usuarios)
                {
                        <div class="carousel-cell">
                            <form method="GET" action="Categoria" id="@usuario.Nombre"> 
                                <input type="hidden" value="@usuario.Id" id="valor">  
                                <a href="javascript:{}" onclick="document.getElementById('@usuario.Nombre').submit();">
                                    <div class="card bg-dark text-white">
                                        <img class="card-img" src="@usuario.imgData" alt="@usuario.Nombre">
                                        <div class="card-img-overlay">
                                            <h5 class="card-title">@usuario.Nombre</h5>
                                        </div>
                                    </div>
                                </a>
                            </form>
                        </div>
                }
        </div>
    </div>
</div>
